module.exports = { createStructure: createStructure };

function createStructure(entity) {
    console.log("criando estrutura");
    // var sentence = "";
    // for (var i = 1; i < entity.length; i++) {
    //     sentence = sentence + entity[i] + " ";
    // }
}